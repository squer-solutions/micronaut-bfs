apiVersion: apps/v1
kind: Deployment
metadata:
  name: "hello-world"
spec:
  selector:
    matchLabels:
      app: "hello-world"
  template:
    metadata:
      labels:
        app: "hello-world"
    spec:
      containers:
        - name: "hello-world"
          image: "gcr.io/hello-world/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "hello-world"
spec:
  selector:
    app: "hello-world"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080