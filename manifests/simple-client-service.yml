apiVersion: v1
kind: Service
metadata:
  name: simple-client-micronaut-app
spec:
  selector:
    app: simple-client-micronaut-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP